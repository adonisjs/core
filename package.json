{
  "name": "adonis-fold",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "istanbul cover _mocha --report lcovonly -- -R spec test/unit && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage",
    "coverage": "istanbul cover _mocha test/unit --bail"
  },
  "author": "adonisjs",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.3.0",
    "coveralls": "^2.11.4",
    "cz-conventional-changelog": "^1.1.4",
    "istanbul": "^0.3.22",
    "mocha": "^2.3.3",
    "mocha-lcov-reporter": "^1.0.0"
  },
  "dependencies": {
    "co": "^4.6.0",
    "co-parallel": "^1.0.0",
    "debug": "^2.2.0",
    "dwell": "^1.0.0",
    "lodash": "^3.10.1",
    "require-stack": "^1.0.1"
  }
}
